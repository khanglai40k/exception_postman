spring:
#  servlet:
#    context-path: /identity
#    cái này ?? nay mai n?u có làm microservice thì nó...  lúc mà postman
  application:
    name: identify-service

  datasource:
    url: jdbc:mysql://localhost:3306/identity_service
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
